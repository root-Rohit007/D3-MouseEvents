const BackGroundCircle = ({ radius }) => (
  <circle
    // cx={width / 2}
    // cy={height / 2}
    r={radius}
    fill="blue"
    stroke="black"
    strokeWidth="5"
  />
);
export default BackGroundCircle;
